#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <functional>

using namespace std;

int main(){
    int n, m;
    scanf("%d %d", &n, &m);
    long long int costsum = 0;
    int * check = new int[n]();
    vector<pair<int,int>> * link = new vector<pair<int,int>> [n];

}